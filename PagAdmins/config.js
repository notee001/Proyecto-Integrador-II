// Las variables de Supabase se cargan directamente en utils.js
// No se puede acceder a .env desde el navegador

console.log('Configuración lista');
let CONFIG = {
  // Las credenciales están en utils.js
};
